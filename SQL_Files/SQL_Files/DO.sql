-- Dictionary Object
-- this is the container 
declare @age int = 45, @fName nvarchar(50) = 'Sara'

-- to see the declare 
select @age as emplyeeAge
select @fName as emplyeeName

declare @studentId int, @fullName nvarchar(50), @dateOfBirth date, @allowence decimal(18,2) 